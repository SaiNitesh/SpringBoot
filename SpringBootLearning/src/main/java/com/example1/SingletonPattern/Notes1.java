package com.example1.SingletonPattern;

public class Notes1 {
	
	/*
	 * 			-------------------
	 *			| Spring Container|
	 *			|                 |
	 *          |                 |
	 *			-------------------
	 **/
	
	/*Spring container has objects which is called 'Spring Bean'
	 * Two scopes when you create a bean 
	 * ->Singleton
	 * ->Prototype
	 * 
	 * By default spring is Singleton protoype. So only once the object will be CREATED
	 * and it will be CALLED each time you run the application.
	 * We can test it by using constructor.
	 * 
	 * 
	 * */

}
