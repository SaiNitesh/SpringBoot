package com.example4.Webapp;

public class Notes4 {
	
	/*
	 * Running the Main call will automatically run all the classes under the package
	 * 
	 * since we added the 'spring web starter' dependency, it will start the inbuilt tomcat server
	 * when you run the application.
	 * 
	 * 
	 * 
	 * In the controller we have a request mapping with path /home
	 * 
	 * After running the application, try to use this url http://localhost:8080/home
	 * It will throw an error since we didnt added any view to display but it will execute the
	 * method mapped with 'home'. check the console and it will diplay 'hi'
	 * 
	 * 
	 * */

}
