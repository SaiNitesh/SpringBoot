package com.example2.ProtoTypePattern;

public class Notes2 {
	
	/*
	 * prototype pattern = Spring will NOT create a bean by default 
	 * and it creates when the 'getBean' method got called.
	 * context.getBean() will create the bean
	 * 
	 * 
	 * You can mention the pattern in the POJO class using @scope annotation
	 * */

}
