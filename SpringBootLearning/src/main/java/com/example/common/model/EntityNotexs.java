package com.example.common.model;

public class EntityNotexs {
	
	//https://www.youtube.com/watch?v=35EQXmHKZYs

	
	/* @Entity(name="MyEntityName")
	   @Table(name="MyEntityTableName")
	   class MyEntity {}
	 * The name in @Entity is for JPA-QL queries, it defaults to the class name without packag
	 * The name in @Table is the table name where this entity is saved.
	 *  
	 *  
	 *  */
}
